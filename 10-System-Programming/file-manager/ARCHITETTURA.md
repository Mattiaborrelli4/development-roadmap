# 🏗️ Architettura del File Manager

## 📊 Diagramma dei Moduli

```
┌─────────────────────────────────────────────────────────┐
│                    FILE MANAGER                          │
│                      Main Loop                           │
└────────────────────────┬────────────────────────────────┘
                         │
         ┌───────────────┼───────────────┐
         │               │               │
    ┌────▼────┐    ┌────▼────┐    ┌────▼────┐
    │  Menu   │    │  Input  │    │  Error  │
    │ System  │    │ Handler │    │ Handler │
    └────┬────┘    └────┬────┘    └────┬────┘
         └───────────────┼───────────────┘
                         │
    ┌────────────────────┼────────────────────┐
    │                    │                    │
┌───▼───┐          ┌────▼────┐          ┌────▼────┐
│ List  │          │  Copy   │          │  Move   │
│ Files │          │  File   │          │  File   │
└───┬───┘          └────┬────┘          └────┬────┘
    └────────────────────┼────────────────────┘
                         │
    ┌────────────────────┼────────────────────┐
    │                    │                    │
┌───▼───┐          ┌────▼────┐          ┌────▼────┐
│Delete │          │  Info   │          │Utility  │
│ File  │          │  File   │          │Functions│
└───────┘          └─────────┘          └─────────┘
    │                    │                    │
    └────────────────────┼────────────────────┘
                         │
                ┌────────▼────────┐
                │  System Calls   │
                │  (POSIX API)    │
                └─────────────────┘
```

## 🔧 Struttura del Codice

### 1. Header & Includes
```c
#include <stdio.h>      // I/O standard
#include <stdlib.h>     // Funzioni generali
#include <string.h>     // Manipolazione stringhe
#include <dirent.h>     // Gestione directory
#include <sys/stat.h>   // Statistiche file
#include <unistd.h>     // System call POSIX
#include <errno.h>      // Gestione errori
```

### 2. Definizioni & Macro
```c
#define MAX_PATH 1024
#define MAX_FILENAME 256
#define COLOR_RESET   "\033[0m"
#define COLOR_RED     "\033[31m"
#define COLOR_GREEN   "\033[32m"
// ... altri colori
```

### 3. Funzioni Utility
```
clear_input_buffer()     // Pulisce buffer input
is_directory()           // Verifica se è directory
file_exists()            // Verifica esistenza file
```

### 4. Funzioni Principali
```
list_files()             // Lista directory corrente
copy_file()              // Copia file
move_file()              // Sposta/rinomina
delete_file()            // Elimina con conferma
show_file_info()         // Mostra info dettagliate
show_current_directory() // Mostra path corrente
```

### 5. Interfaccia Utente
```
display_menu()           // Mostra menu principale
main()                   // Loop principale
```

## 🔄 Flow Chart Operazioni

### Operazione di Copia
```
┌─────────┐
│  Start  │
└────┬────┘
     │
     ▼
┌─────────────────┐
│ Input Source    │
│ & Destination   │
└────┬────────────┘
     │
     ▼
┌─────────────────┐     NO     ┌──────────────┐
│ Source Exists?  │──────────►│ Print Error  │
└────┬────────────┘           └──────────────┘
     │ YES
     ▼
┌─────────────────┐     NO     ┌──────────────┐
│ Source is File? │──────────►│ Print Error  │
└────┬────────────┘           └──────────────┘
     │ YES
     ▼
┌─────────────────┐
│ Open Source     │
│ (read mode)     │
└────┬────────────┘
     │
     ▼
┌─────────────────┐
│ Open Dest       │
│ (write mode)    │
└────┬────────────┘
     │
     ▼
┌─────────────────┐
│ Copy with       │
│ Buffer (4KB)    │
└────┬────────────┘
     │
     ▼
┌─────────────────┐
│ Close Files     │
└────┬────────────┘
     │
     ▼
┌─────────────────┐
│ Print Success   │
└────┬────────────┘
     │
     ▼
┌─────────┐
│  End    │
└─────────┘
```

### Operazione di Eliminazione
```
┌─────────┐
│  Start  │
└────┬────┘
     │
     ▼
┌─────────────────┐     NO     ┌──────────────┐
│ File Exists?    │──────────►│ Print Error  │
└────┬────────────┘           └──────────────┘
     │ YES
     ▼
┌─────────────────┐     NO     ┌──────────────┐
│ Is Directory?   │──────────►│ Print Error  │
└────┬────────────┘           └──────────────┘
     │ YES (File)
     ▼
┌─────────────────┐
│ Ask Confirm     │
│ (s/n)           │
└────┬────────────┘
     │
     ▼
┌─────────────────┐     NO     ┌──────────────┐
│ Confirmed?      │──────────►│ Cancel & End │
└────┬────────────┘           └──────────────┘
     │ YES
     ▼
┌─────────────────┐
│ unlink() Call   │
└────┬────────────┘
     │
     ▼
┌─────────────────┐
│ Print Result    │
└────┬────────────┘
     │
     ▼
┌─────────┐
│  End    │
└─────────┘
```

## 📋 Tabella System Call

| System Call | Libreria | Uso nel Progetto |
|-------------|----------|------------------|
| `opendir()` | `dirent.h` | Apri directory |
| `readdir()` | `dirent.h` | Leggi entry directory |
| `closedir()` | `dirent.h` | Chiudi directory |
| `stat()` | `sys/stat.h` | Ottieni info file |
| `fopen()` | `stdio.h` | Apri file |
| `fread()` | `stdio.h` | Leggi da file |
| `fwrite()` | `stdio.h` | Scrivi su file |
| `fclose()` | `stdio.h` | Chiudi file |
| `rename()` | `stdio.h` | Sposta/Rinomina |
| `unlink()` | `unistd.h` | Elimina file |
| `getcwd()` | `unistd.h` | Directory corrente |

## 🎯 Pattern di Progetto

### 1. Input Sanitization
```c
fgets(buffer, size, stdin);
buffer[strcspn(buffer, "\n")] = 0;  // Rimuove newline
clear_input_buffer();                // Pulisce buffer
```

### 2. Error Handling
```c
if (operation_failed) {
    printf(COLOR_RED "Errore: %s\n" COLOR_RESET, strerror(errno));
    return 0;
}
```

### 3. Resource Management
```c
FILE *file = fopen(...);
if (file == NULL) {
    // Gestione errore
}
// ... operazioni ...
fclose(file);  // Sempra chiusura
```

## 🚀 Ottimizzazioni

1. **Buffer Size**: 4KB per ottimizzare I/O
2. **Single System Call**: `stat()` invece di multiple call
3. **Early Returns**: Riduce nidificazione
4. **Input Validation**: Previene buffer overflow

## 📊 Complessità

| Operazione | Complessità Temporale | Complessità Spaziale |
|------------|----------------------|---------------------|
| List Files | O(n) | O(1) |
| Copy File | O(n) | O(1) |
| Move File | O(1) | O(1) |
| Delete File | O(1) | O(1) |
| File Info | O(1) | O(1) |

Dove n = numero di file nella directory
