# 📊 Diagramma delle Classi - Snake Game

## Visualizzazione Architettura

```
┌─────────────────────────────────────────────────────────────────┐
│                         main()                                   │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                    GAME LOOP                            │   │
│  │                                                          │   │
│  │  ┌──────────┐    ┌──────────┐    ┌──────────┐         │   │
│  │  │  INPUT   │───▶│  UPDATE  │───▶│  RENDER  │         │   │
│  │  └──────────┘    └──────────┘    └──────────┘         │   │
│  │       ▲                                    │            │   │
│  │       └────────────────────────────────────┘            │   │
│  │                   Finché giocoTerminato == false         │   │
│  └─────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
                           │
                           │ crea istanza
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│                      Classe GiocoSnake                          │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                    MEMBRI PRIVATI                       │   │
│  │  ┌─────────────────────────────────────────────────┐   │   │
│  │  │ int larghezza, altezza                           │   │   │
│  │  │ deque<Posizione> serpente                        │   │   │
│  │  │ enum Direzione {SU, GIU, SINISTRA, DESTRA}       │   │   │
│  │  │ Posizione cibo                                   │   │   │
│  │  │ bool giocoTerminato                              │   │   │
│  │  │ int punteggio                                     │   │   │
│  │  │ mt19937 gen (generatore casuale)                 │   │   │
│  │  └─────────────────────────────────────────────────┘   │   │
│  ├─────────────────────────────────────────────────────────┤   │
│  │                  METODI PUBBLICI                        │   │
│  │  ┌─────────────────────────────────────────────────┐   │   │
│  │  │ + GiocoSnake(w, h)              [costruttore]    │   │   │
│  │  │ + ~GiocoSnake()                   [distruttore]  │   │   │
│  │  │ + aggiorna()                       [void]         │   │   │
│  │  │ + processaInput(tasto)            [void]         │   │   │
│  │  │ + renderizza()        const        [void]         │   │   │
│  │  │ + èTerminato()         const        [bool]         │   │   │
│  │  │ + getPunteggio()       const        [int]          │   │   │
│  │  └─────────────────────────────────────────────────┘   │   │
│  ├─────────────────────────────────────────────────────────┤   │
│  │                  METODI PRIVATI                          │   │
│  │  ┌─────────────────────────────────────────────────┐   │   │
│  │  │ - generaCibo()                      [void]        │   │   │
│  │  │ - èFuoriBordi(pos)        const       [bool]        │   │   │
│  │  │ - verificaCollisione()     const       [bool]        │   │   │
│  │  │ - calcolaNuovaTesta()      const       [Posizione]   │   │   │
│  │  └─────────────────────────────────────────────────┘   │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  Usa/Composizione:                                              │
│  ┌──────────────┐    ┌──────────────┐    ┌──────────────┐     │
│  │ deque<       │    │ vector<      │    │ Posizione    │     │
│  │ Posizione>   │    │ Posizione>   │    │              │     │
│  └──────────────┘    └──────────────┘    └──────────────┘     │
└─────────────────────────────────────────────────────────────────┘
                           │
                           │ contiene molti
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│                      Classe Posizione                           │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                    MEMBRI PRIVATI                       │   │
│  │  ┌─────────────────────────────────────────────────┐   │   │
│  │  │ int x                                           │   │   │
│  │  │ int y                                           │   │   │
│  │  └─────────────────────────────────────────────────┘   │   │
│  ├─────────────────────────────────────────────────────────┤   │
│  │                  METODI PUBBLICI                        │   │
│  │  ┌─────────────────────────────────────────────────┐   │   │
│  │  │ + Posizione(x=0, y=0)           [costruttore]    │   │   │
│  │  │ + getX()            const        [int]            │   │   │
│  │  │ + getY()            const        [int]            │   │   │
│  │  │ + setX(nuovoX)                   [void]           │   │   │
│  │  │ + setY(nuovoY)                   [void]           │   │   │
│  │  │ + operator==(altra)  const       [bool]           │   │   │
│  │  └─────────────────────────────────────────────────┘   │   │
│  └─────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
```

## Relazioni tra Classi

```
┌──────────────┐
│   main()     │
└──────┬───────┘
       │ 1
       │ crea
       │
       ▼
┌──────────────────┐
│  GiocoSnake      │
└────┬────────┬────┘
     │        │
     │        │ contiene
     │        │ (composizione)
     │        ▼
     │  ┌──────────────┐       1..*
     │  │  Posizione   ├─────────────────┐
     │  └──────────────┘                 │
     │                                  │
     │ usa (STL containers)              │
     ▼                                  │
┌─────────────────────────────────────────────────────────────┐
│                         STL                                 │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐        │
│  │  deque<T>   │  │  vector<T>  │  │  random     │        │
│  │             │  │             │  │  engine     │        │
│  └─────────────┘  └─────────────┘  └─────────────┘        │
└─────────────────────────────────────────────────────────────┘
```

## Flusso di Dati

```
┌──────────┐
│  UTENTE  │
└────┬─────┘
     │ input (WASD)
     ▼
┌──────────────┐
│ processaInput│
└──────┬───────┘
       │ aggiorna direzione
       ▼
┌──────────────┐
│   aggiorna   │◀────────────────────────────┐
└──────┬───────┘                              │
       │                                      │
       │ calcola nuova posizione              │
       ▼                                      │
┌──────────────────┐                          │
│ calcolaNuovaTesta│                          │
└──────┬───────────┘                          │
       │                                      │
       │ nuova posizione                      │
       ▼                                      │
┌──────────────┐    ┌──────────────────┐     │
│èFuoriBordi   │ NO │verificaCollisione│─────┤
└──────┬───────┘    └──────┬───────────┘     │
       │ YES                │ NO             │
       ▼                    ▼                │
┌──────────────┐    ┌──────────────┐         │
│game over     │    │  crescere    │         │
└──────────────┘    │  (mangiato)  │         │
                    └──────┬───────┘         │
                           │ YES             │
                           ▼                 │
                    ┌──────────────┐         │
                    │ generaCibo   │         │
                    └──────────────┘         │
                           │                 │
                           └─────────────────┘
                                      │
                                      │ stato aggiornato
                                      ▼
                             ┌──────────────┐
                             │  renderizza  │
                             └──────────────┘
                                      │
                                      │ output ASCII
                                      ▼
                             ┌──────────────┐
                             │   UTENTE     │
                             └──────────────┘
```

## Diagramma Stati del Serpente

```
┌──────────────────────────────────────────────────────────┐
│                  STATI DEL GIOCO                         │
└──────────────────────────────────────────────────────────┘

INIZIO
  │
  ├─→ Serpente creato (3 segmenti)
  ├─→ Posizione: centro griglia
  ├─→ Direzione: DESTRA
  └─→ Cibo generato casualmente

DURANTE IL GIOCO
  │
  ├─→ Movimento:
  │   ├─ Testa si muove nella direzione corrente
  │   ├─ Corpo segue (ogni segmento prende posizione precedente)
  │   └─ Coda viene rimossa (se non mangia)
  │
  ├─→ Mangiando cibo:
  │   ├─ Punteggio +10
  │   ├─ Serpente cresce (coda non rimossa)
  │   └─ Nuovo cibo generato
  │
  └─→ Collisioni:
      ├─ Muri → Game Over
      └─ Se stesso → Game Over

GAME OVER
  │
  ├─→ Mostra punteggio finale
  ├─→ Messaggio di chiusura
  └─→ Programma termina
```

## Template Method Pattern

Il `GiocoSnake` segue implicitamente il Template Method Pattern:

```
templateMethod() {
    while (!isDone()) {
        input();    // Metodo hook
        update();   // Metodo astratto
        render();   // Metodo hook
    }
}
```

## Strategy Pattern (Potenziale)

La direzione potrebbe essere implementata con Strategy Pattern:

```
┌──────────────────┐
│   Direzione      │ (Strategy Interface)
│  ┌────────────┐  │
│  │muovi()     │  │
│  └────────────┘  │
└──────────────────┘
         ▲
         │
    ┌────┴─────┬──────────┬──────────┐
    │          │          │          │
┌───┴───┐ ┌───┴───┐ ┌────┴───┐ ┌───┴───┐
│   SU  │ │ GIÙ   │ │SINISTRA│ │DESTRA │
└───────┘ └───────┘ └────────┘ └───────┘
```

## OOP Principles Dimostrati

### 1. Incapsulamento
```
┌─────────────────────────────┐
│      GiocoSnake             │
│  ┌───────────────────────┐  │
│  │  PRIVATE:             │  │
│  │  - larghezza          │  │
│  │  - altezza            │  │
│  │  - serpente           │  │
│  │  - punteggio          │  │
│  └───────────────────────┘  │
│  ┌───────────────────────┐  │
│  │  PUBLIC:              │  │
│  │  + aggiorna()         │  │
│  │  + renderizza()       │  │
│  │  + getPunteggio()     │  │
│  └───────────────────────┘  │
└─────────────────────────────┘
```

### 2. Single Responsibility
```
Posizione      → Rappresenta coordinate 2D
GiocoSnake     → Gestisce logica del gioco
main()         → Gestisce flusso programma
```

### 3. Open/Closed Principle
```
Il gioco è aperto all'estensione (nuovi metodi)
ma chiuso alla modifica (interfaccia stabile)
```

### 4. Dependency Inversion
```
main() dipende dall'astrazione GiocoSnake
non dall'implementazione interna
```

---

**Creato per aiutare a visualizzare l'architettura del codice**

Per una migliore visualizzazione, consiglia di usare tool come:
- PlantUML
- Mermaid
- draw.io
- Visual Studio Code Diagrams
