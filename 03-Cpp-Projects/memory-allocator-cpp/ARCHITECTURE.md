# C++ Memory Allocator - Architecture Documentation

## 🏗️ System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                     Application Layer                          │
│  (Uses MY_MALLOC, MY_FREE, MY_REALLOC macros)                 │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                   Allocator Interface                          │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐        │
│  │ my_malloc()  │  │  my_free()   │  │ my_realloc() │        │
│  └──────────────┘  └──────────────┘  └──────────────┘        │
└────────────────────────┬────────────────────────────────────────┘
                         │
                ┌────────┴────────┐
                │                 │
                ▼                 ▼
┌──────────────────────────┐  ┌──────────────────────────┐
│    Memory Pool (Fast)    │  │    General Allocator    │
│  ┌────────────────────┐  │  │  ┌────────────────────┐│
│  │ Pool 1: 16 bytes  │  │  │  │  First-Fit Search  ││
│  │ Pool 2: 32 bytes  │  │  │  │  Block Splitting   ││
│  │ Pool 3: 64 bytes  │  │  │  │  Coalescing        ││
│  │ Pool 4: 128 bytes │  │  │  │  System Memory     ││
│  │ Pool 5: 256 bytes │  │  │  │  Management        ││
│  │ Pool 6: 512 bytes │  │  │  └────────────────────┘│
│  └────────────────────┘  │  └──────────────────────────┘
│         O(1) allocation    │         O(n) allocation
└──────────────────────────┴──────────────────────────┘
                │                 │
                └────────┬────────┘
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                      System Memory                              │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐          │
│  │ Block 1 │  │ Block 2 │  │ Block 3 │  │ Block N │ ...      │
│  └─────────┘  └─────────┘  └─────────┘  └─────────┘          │
└─────────────────────────────────────────────────────────────────┘
```

## 🧱 Block Structure

### Physical Memory Layout

```
┌──────────────────────────────────────────────────────────────┐
│ Block 1                                                      │
│ ┌────────────────────────────────────────────────────────┐  │
│ │ BlockMetadata                                          │  │
│ │ magic: 0xDEADBEEF                                      │  │
│ │ size: 100 bytes                                        │  │
│ │ is_free: false                                         │  │
│ │ prev: nullptr                                          │  │
│ │ next: → Block 2                                        │  │
│ │ allocation_id: 1                                       │  │
│ ├────────────────────────────────────────────────────────┤  │
│ │ User Data (100 bytes)                                  │  │
│ │                                                       │  │
│ └────────────────────────────────────────────────────────┘  │
├──────────────────────────────────────────────────────────────┤
│ Block 2                                                      │
│ ┌────────────────────────────────────────────────────────┐  │
│ │ BlockMetadata                                          │  │
│ │ magic: 0xDEADBEEF                                      │  │
│ │ size: 200 bytes                                        │  │
│ │ is_free: true                                          │  │
│ │ prev: ← Block 1                                        │  │
│ │ next: → Block 3                                        │  │
│ │ allocation_id: 0                                       │  │
│ ├────────────────────────────────────────────────────────┤  │
│ │ Free Space (200 bytes)                                 │  │
│ │                                                       │  │
│ └────────────────────────────────────────────────────────┘  │
├──────────────────────────────────────────────────────────────┤
│ Block 3                                                      │
│ ┌────────────────────────────────────────────────────────┐  │
│ │ BlockMetadata                                          │  │
│ │ magic: 0xDEADBEEF                                      │  │
│ │ size: 50 bytes                                         │  │
│ │ is_free: false                                         │  │
│ │ prev: ← Block 2                                        │  │
│ │ next: nullptr                                          │  │
│ │ allocation_id: 2                                       │  │
│ ├────────────────────────────────────────────────────────┤  │
│ │ User Data (50 bytes)                                   │  │
│ │                                                       │  │
│ └────────────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────────┘
```

## 🔀 Allocation Flow

### my_malloc(size)

```
START
  │
  ├─ size < 512?
  │   ├─ YES → Use Memory Pool
  │   │         │
  │   │         ├─ Find appropriate pool
  │   │         ├─ Get block from free list [O(1)]
  │   │         ├─ Track allocation
  │   │         └─ Return pointer
  │   │
  │   └─ NO → Use General Allocator
  │             │
  │             ├─ Find free block (first-fit) [O(n)]
  │             │   │
  │             │   ├─ Found?
  │             │   │   ├─ YES → Continue
  │             │   │   └─ NO → Request more system memory
  │             │   │
  │             ├─ Split block if too large
  │             ├─ Mark as used
  │             ├─ Track allocation
  │             └─ Return pointer
  │
END
```

### my_free(ptr)

```
START
  │
  ├─ From pool?
  │   ├─ YES → Return to pool free list [O(1)]
  │
  └─ NO → General allocator free
           │
           ├─ Validate header (magic check)
           ├─ Check double-free
           ├─ Mark as free
           ├─ Coalesce with adjacent blocks
           │   │
           │   ├─ Previous block free? → Merge
           │   └─ Next block free? → Merge
           │
           ├─ Remove from tracking
           └─ Update statistics
  │
END
```

## 🎯 Memory Pool Structure

### Fixed-Size Pool Layout

```
┌─────────────────────────────────────────────────────────┐
│ Pool (32-byte blocks)                                   │
│ ┌─────────────────────────────────────────────────────┐│
│ │ Chunk 1 (4096 bytes)                                ││
│ │ ┌────┬────┬────┬────┬────┬────┬────┬────┐         ││
│ │ │ B1 │ B2 │ B3 │ B4 │ B5 │... │BN-2│BN-1│ 128x32B││
│ │ └────┴────┴────┴────┴────┴────┴────┴────┘         ││
│ └─────────────────────────────────────────────────────┘│
│ ┌─────────────────────────────────────────────────────┐│
│ │ Chunk 2 (4096 bytes)                                ││
│ │ ┌────┬────┬────┬────┬────┬────┬────┬────┐         ││
│ │ │ B1 │ B2 │ B3 │ B4 │ B5 │... │BN-2│BN-1 │ 128x32B││
│ │ └────┴────┴────┴────┴────┴────┴────┴────┘         ││
│ └─────────────────────────────────────────────────────┘│
│ ┌─────────────────────────────────────────────────────┐│
│ │ Free List (singly linked)                           ││
│ │ ┌───┐   ┌───┐   ┌───┐                             ││
│ │ │   │→  │   │→  │   │→  nullptr                    ││
│ │ └───┘   └───┘   └───┘                             ││
│ └─────────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────────┘
```

### Pool Selection Logic

```
Request Size → Pool Selection
     │
     ├─ 0-16 bytes   → Pool 1 (16B)
     ├─ 17-32 bytes  → Pool 2 (32B)
     ├─ 33-64 bytes  → Pool 3 (64B)
     ├─ 65-128 bytes → Pool 4 (128B)
     ├─ 129-256 bytes→ Pool 5 (256B)
     ├─ 257-512 bytes→ Pool 6 (512B)
     └─ > 512 bytes  → General Allocator
```

## 🔒 Safety Mechanisms

### Magic Number Validation

```
┌─────────────────────────────────────────────────────────┐
│ Block State Transitions                                 │
│                                                         │
│  INIT → 0xDEADBEEF (VALID)                              │
│    │                                                     │
│    ├─ ALLOCATE → magic = 0xDEADBEEF, is_free = false   │
│    │                                                     │
│    ├─ FREE → magic = 0xCAFEBABE, is_free = true        │
│    │                                                     │
│    └─ DOUBLE-FREE → magic = 0xFEEEFEEE                 │
│                                                         │
│ Validation Checks:                                       │
│  - magic == 0xDEADBEEF → Valid allocated block           │
│  - magic == 0xCAFEBABE → Use-after-free warning         │
│  - magic == 0xFEEEFEEE → Double-free error               │
│  - magic == other → Header corruption                   │
└─────────────────────────────────────────────────────────┘
```

### Coalescing Process

```
Before Coalescing:
┌──────┬──────┬──────┬──────┐
│ USED │ FREE │ FREE │ USED │
└──────┴──────┴──────┴──────┘

After freeing USED:
┌──────┬──────┬──────┬──────┐
│ FREE │ FREE │ FREE │ USED │
└──────┴──────┴──────┴──────┘

After Coalescing:
┌─────────────┬─────────────┐
│    FREE     │    USED     │
└─────────────┴─────────────┘
    (3 blocks merged)
```

## 📊 Statistics Tracking

```
AllocatorStats Structure:
┌────────────────────────────────┐
│ total_allocated                │  ← Total bytes in use
│ total_free                    │  ← Total bytes available
│ free_blocks                   │  ← Number of free blocks
│ total_blocks                  │  ← Total blocks in heap
│ system_memory                 │  ← Memory from system
└────────────────────────────────┘

Allocation Tracking:
┌────────────────────────────────┐
│ allocations (map)              │
│  ┌─────────────────────────┐   │
│  │ ptr → AllocationInfo   │   │
│  │  - address             │   │
│  │  - size                │   │
│  │  - file:line           │   │
│  │  - allocation_id       │   │
│  └─────────────────────────┘   │
└────────────────────────────────┘

Leak Detection:
- Check allocations map at exit
- Report all non-freed allocations
- Show file:line of allocation
```

## 🎨 Class Hierarchy

```
MemoryAllocator Namespace
├── Allocator (Singleton)
│   ├── my_malloc()
│   ├── my_free()
│   ├── my_realloc()
│   ├── find_free_block()
│   ├── split_block()
│   ├── coalesce_blocks()
│   ├── validate_block()
│   ├── detect_memory_leaks()
│   └── get_stats()
│
├── MemoryPool (Singleton)
│   ├── allocate()
│   ├── deallocate()
│   ├── is_from_pool()
│   └── find_pool()
│
└── FixedSizePool
    ├── allocate()
    ├── deallocate()
    ├── is_from_pool()
    └── allocate_chunk()

Utilities:
├── Debug (Static Class)
│   ├── log()
│   ├── log_alloc()
│   ├── log_free()
│   └── log_error()
│
├── Timer
│   ├── reset()
│   ├── elapsed_ms()
│   └── elapsed_us()
│
└── AllocationTracker
    ├── add_allocation()
    ├── remove_allocation()
    └── print_leaks()
```

## 🚀 Performance Characteristics

| Operation | Complexity | Notes |
|-----------|-----------|-------|
| Pool malloc | O(1) | Direct free list access |
| Pool free | O(1) | Return to free list |
| General malloc | O(n) | First-fit search |
| General free | O(1) | Coalescing O(1) |
| Realloc | O(n) | May require memcpy |

## 🔧 Platform Abstraction

```
Platform-Specific Memory Allocation:

Windows:
├── VirtualAlloc()
│   └── MEM_COMMIT | MEM_RESERVE
│       PAGE_READWRITE
│
└── VirtualFree()
    └── MEM_RELEASE

Unix/Linux/macOS:
├── mmap()
│   ├── PROT_READ | PROT_WRITE
│   └── MAP_PRIVATE | MAP_ANONYMOUS
│
└── munmap()

Cross-Platform Abstraction:
request_memory(size) → void*
    └── Internally uses VirtualAlloc/mmap
```

---

**Document Version**: 1.0
**Last Updated**: 2026-02-12
**Language**: English
