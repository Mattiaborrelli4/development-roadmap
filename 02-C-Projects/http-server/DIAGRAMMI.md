# 🔄 Architettura del Server HTTP

## Diagramma di Flusso

```
┌──────────────────────────────────────────────────────────────┐
│                         CLIENT (Browser)                      │
└────────────────────────────┬─────────────────────────────────┘
                             │
                             │ HTTP GET Request
                             │
                             ▼
┌──────────────────────────────────────────────────────────────┐
│                      TCP Socket (8080)                        │
│  ┌────────────────────────────────────────────────────────┐  │
│  │  accept() - Accetta nuova connessione                 │  │
│  └────────────────────────────────────────────────────────┘  │
└────────────────────────────┬─────────────────────────────────┘
                             │
                             │ Creazione Thread
                             ▼
┌──────────────────────────────────────────────────────────────┐
│                    CLIENT THREAD                              │
│  ┌────────────────────────────────────────────────────────┐  │
│  │  1. recv() - Ricevi richiesta HTTP                     │  │
│  └────────────────────────────────────────────────────────┘  │
│                              │                                 │
│                              ▼                                 │
│  ┌────────────────────────────────────────────────────────┐  │
│  │  2. Parse HTTP Request                                │  │
│  │     - Method (GET)                                     │  │
│  │     - Path (/index.html)                               │  │
│  │     - Version (HTTP/1.1)                               │  │
│  └────────────────────────────────────────────────────────┘  │
│                              │                                 │
│                              ▼                                 │
│  ┌────────────────────────────────────────────────────────┐  │
│  │  3. Validazione                                        │  │
│  │     - Method == GET?                                   │  │
│  │     - Path sanitizzato?                                │  │
│  └────────────────────────────────────────────────────────┘  │
│                              │                                 │
│                              ▼                                 │
│  ┌────────────────────────────────────────────────────────┐  │
│  │  4. File Serving                                       │  │
│  │     - Apri file (./public/path)                        │  │
│  │     - Leggi contenuto                                  │  │
│  │     - Determina Content-Type                          │  │
│  └────────────────────────────────────────────────────────┘  │
│                              │                                 │
│                              ▼                                 │
│  ┌────────────────────────────────────────────────────────┐  │
│  │  5. Costruisci Response                                │  │
│  │     HTTP/1.1 200 OK\r\n                                │  │
│  │     Content-Type: text/html\r\n                       │  │
│  │     Content-Length: 1234\r\n                          │  │
│  │     \r\n                                               │  │
│  │     [file content]                                     │  │
│  └────────────────────────────────────────────────────────┘  │
│                              │                                 │
│                              ▼                                 │
│  ┌────────────────────────────────────────────────────────┐  │
│  │  6. send() - Invia response                            │  │
│  └────────────────────────────────────────────────────────┘  │
│                              │                                 │
│                              ▼                                 │
│  ┌────────────────────────────────────────────────────────┐  │
│  │  7. close() - Chiudi socket                            │  │
│  └────────────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────────┘
```

## Protocollo HTTP

### Request Format
```
GET /path/to/file.html HTTP/1.1\r\n
Host: localhost:8080\r\n
User-Agent: Mozilla/5.0\r\n
Accept: */*\r\n
\r\n
```

### Response Format
```
HTTP/1.1 200 OK\r\n
Content-Type: text/html\r\n
Content-Length: 1234\r\n
Connection: close\r\n
Server: C-HTTP-Server/1.0\r\n
\r\n
<!DOCTYPE html>...
```

## Content-Type Mapping

```
┌─────────────────┬─────────────────────────────┐
│ Estensione     │ Content-Type                │
├─────────────────┼─────────────────────────────┤
│ .html, .htm    │ text/html                   │
│ .css           │ text/css                    │
│ .js            │ application/javascript      │
│ .jpg, .jpeg    │ image/jpeg                  │
│ .png           │ image/png                   │
│ .gif           │ image/gif                   │
│ .svg           │ image/svg+xml               │
│ .json          │ application/json            │
│ .txt           │ text/plain                  │
│ .pdf           │ application/pdf             │
│ .ico           │ image/x-icon                │
│ .woff          │ font/woff                   │
└─────────────────┴─────────────────────────────┘
```

## Codici di Risposta HTTP

```
┌──────┬─────────────────┬────────────────────────────┐
│ Code │ Status         │ Descrizione                │
├──────┼─────────────────┼────────────────────────────┤
│ 200  │ OK             │ File trovato e servito      │
│ 404  │ Not Found      │ File non esistente          │
│ 405  │ Not Allowed    │ Metodo non supportato       │
│ 500  │ Server Error   │ Errore interno server       │
└──────┴─────────────────┴────────────────────────────┘
```

## Threading Model

```
Main Thread
    │
    ├───> Accept Connection 1 ──> Thread 1 ──> Handle ──> Close
    │
    ├───> Accept Connection 2 ──> Thread 2 ──> Handle ──> Close
    │
    ├───> Accept Connection 3 ──> Thread 3 ──> Handle ──> Close
    │
    └───> ... (continua loop)
```

## Gestione degli Errori

```
recv() fails
    │
    ▼
Chiudi socket
    │
    ▼
Termina thread


fopen() fails
    │
    ▼
Send 404 Response
    │
    ▼
Chiudi socket
    │
    ▼
Termina thread


Method != GET
    │
    ▼
Send 405 Response
    │
    ▼
Chiudi socket
    │
    ▼
Termina thread
```

## Security

### Directory Traversal Protection
```
Input:  /../../../etc/passwd
        │
        ▼
Sanitize: /___/___/___/etc/passwd
        │
        ▼
Final: ./public/___/___/___/etc/passwd
        │
        ▼
Result: 404 Not Found
```

### Buffer Overflow Prevention
```
✅ snprintf() instead of sprintf()
✅ strncpy() instead of strcpy()
✅ Fixed buffer sizes (BUFFER_SIZE)
```

## Performance Considerations

### Otimizzazioni Implementate
1. **Thread Pool**: Un thread per connessione
2. **Binary Mode**: Lettura file in modalità binaria
3. **Memory Buffer**: Lettura completa file in memoria
4. **Connection Close**: Chiudi subito dopo la risposta

### Possibili Miglioramenti
1. **Keep-Alive**: Riuso connessioni
2. **Caching**: Cache file in memoria
3. **Thread Pool**: Pool di thread pre-allocati
4. **epoll/kqueue**: Scalabilità migliore
5. **Sendfile**: Zero-copy per file

## Cross-Platform

```
┌─────────────────┬─────────────────┬─────────────────┐
│ Feature        │ Windows         │ Linux           │
├─────────────────┼─────────────────┼─────────────────┤
│ Socket API      │ Winsock2        │ BSD Sockets     │
│ Thread API      │ CreateThread    │ pthread         │
│ Init            │ WSAStartup      │ (auto)          │
│ Cleanup         │ WSACleanup      │ (auto)          │
│ Close Socket    │ closesocket     │ close           │
└─────────────────┴─────────────────┴─────────────────┘
```

## Funzioni Chiave

```
socket()      → Crea socket TCP
bind()        → Associa socket a porta
listen()      → Metti socket in ascolto
accept()      → Accetta connessione client
recv()        → Ricevi dati
strtok()      → Parse HTTP request
fopen()       → Apri file
fread()       → Leggi file
snprintf()    → Costruisci response
send()        → Invia response
close()       → Chiudi socket
```

## File System Layout

```
http-server/
├── server.c          ← Codice sorgente
├── server.exe       ← Eseguibile (Windows)
├── Makefile         ← Build automation
└── public/          ← Web root
    ├── index.html   ← Homepage
    ├── style.css    ← Styles
    ├── script.js    ← JavaScript
    ├── test.html    ← Test page
    └── page2.html   ← Second page
```

Questo server è perfetto per:
- ✅ Apprendere Socket Programming
- ✅ Capire il Protocollo HTTP
- ✅ Studiare Multi-threading
- ✅ Cross-platform Development
- ✅ Sistemi Operativi e Reti
