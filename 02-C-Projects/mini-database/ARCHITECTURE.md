# 🏗️ Architettura Mini Database Engine

## Struttura ad Alti Livelli

```
┌─────────────────────────────────────────────────────────┐
│                    User (CLI)                            │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────┐
│                      main.c                             │
│  ┌───────────────────────────────────────────────────┐  │
│  │  - Command Parser                                  │  │
│  │  - Interactive Loop                                │  │
│  │  - Auto Save/Load                                  │  │
│  │  - Error Handling                                  │  │
│  └───────────────────────────────────────────────────┘  │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────┐
│                   database.c                            │
│  ┌───────────────────────────────────────────────────┐  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  Hash Table Operations                      │  │  │
│  │  │  - db_create()                              │  │  │
│  │  │  - db_destroy()                             │  │  │
│  │  │  - hash_function()                          │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  │                                                     │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  CRUD Operations                            │  │  │
│  │  │  - db_set()    [Create/Update]             │  │  │
│  │  │  - db_get()    [Read]                       │  │  │
│  │  │  - db_delete() [Delete]                     │  │  │
│  │  │  - db_list()   [Read All]                   │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  │                                                     │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  Persistence Layer                           │  │  │
│  │  │  - db_save()   [Write Binary]               │  │  │
│  │  │  - db_load()   [Read Binary]                │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────┘  │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────┐
│                   database.h                             │
│  - Type Definitions (Record, HashTable)                  │
│  - Constants (MAX_KEY_LENGTH, TABLE_SIZE, etc.)          │
│  - Function Prototypes                                   │
└─────────────────────────────────────────────────────────┘
```

## Struttura Dati

### Record
```
┌──────────────────────────────────────┐
│  Record (64 bytes)                   │
├──────────────────────────────────────┤
│  key[64]     │ "nome"                │  Chiave
│  value[256]  │ "Mario Rossi"         │  Valore
│  timestamp   │ 1739380800            │  Unix time
└──────────────────────────────────────┘
```

### Hash Table
```
┌─────────────────────────────────────────────────────────┐
│  HashTable                                              │
│  ┌─────┬─────┬─────┬─────┬─────┬─────┬─────┬─────┐      │
│  │  0  │  1  │  2  │  3  │  4  │  5  │  6  │ ... │      │
│  ├─────┼─────┼─────┼─────┼─────┼─────┼─────┼─────┤      │
│  │NULL │ R1  │NULL │ R2  │NULL │ R3  │NULL │ ... │      │
│  └─────┴─────┴─────┴─────┴─────┴─────┴─────┴─────┘      │
│                                                          │
│  count: 3                                                │
│  size: 1024                                              │
└─────────────────────────────────────────────────────────┘
```

## Flusso di Esecuzione

### Operazione SET
```
User Input: "SET nome Mario"
       │
       ▼
Parse Command → cmd="SET", key="nome", value="Mario"
       │
       ▼
hash_function("nome") → index = 42
       │
       ▼
Check Collision → table[42] == NULL?
       │                    │
       │ NO                │ YES
       ▼                    ▼
Key exists?          Create Record*
       │                    │
       │ YES                │
       ▼                    ▼
Update Value        Insert at index
       │                    │
       └──────────┬─────────┘
                  │
                  ▼
        Increment count
                  │
                  ▼
            Return SUCCESS
```

### Operazione GET
```
User Input: "GET nome"
       │
       ▼
Parse Command → cmd="GET", key="nome"
       │
       ▼
hash_function("nome") → index = 42
       │
       ▼
Check table[42]
       │
       ▼
Record exists && key matches?
       │                    │
       │ NO                │ YES
       ▼                    ▼
Probe next index    Return value
       │
       ▼
Return NOT_FOUND
```

## Gestione Collisioni (Linear Probing)

```
Hash: h(key) = (hash * 33 + c) % 1024

Esempio Collisione:
┌─────┬─────┬─────┬─────┬─────┐
│  0  │  1  │  2  │  3  │ ... │
├─────┼─────┼─────┼─────┼─────┤
│ R1  │ R2  │NULL │     │     │  ← Slot 0,1 occupati
└─────┴─────┴─────┴─────┴─────┘

SET key3 → hash(key3) = 1
           Check table[1] = R2 (collisione!)
           Check table[2] = NULL ✓
           Insert at table[2]

Risultato:
┌─────┬─────┬─────┬─────┬─────┐
│  0  │  1  │  2  │  3  │ ... │
├─────┼─────┼─────┼─────┼─────┤
│ R1  │ R2  │ R3  │     │     │
└─────┴─────┴─────┴─────┴─────┘
```

## Formato File Binario

```
┌──────────────────────────────────────────────┐
│              database.db                       │
├──────────────────────────────────────────────┤
│ Header:                                       │
│   count (int, 4 bytes) = 3                    │
├──────────────────────────────────────────────┤
│ Records:                                      │
│   ┌────────────────────────────────┐          │
│   │ Record 1 (324 bytes)            │          │
│   │  - key[64]                     │          │
│   │  - value[256]                  │          │
│   │  - timestamp (8 bytes)         │          │
│   └────────────────────────────────┘          │
│   ┌────────────────────────────────┐          │
│   │ Record 2 (324 bytes)            │          │
│   └────────────────────────────────┘          │
│   ┌────────────────────────────────┐          │
│   │ Record 3 (324 bytes)            │          │
│   └────────────────────────────────┘          │
└──────────────────────────────────────────────┘

Dimensione totale = 4 + (324 × num_record)
```

## Complessità Algoritmica

| Operazione | Caso Medio | Caso Peggiore | Note |
|-----------|-----------|--------------|-------|
| SET | O(1) | O(n) | Linear probing |
| GET | O(1) | O(n) | Linear probing |
| DELETE | O(1) | O(n) | Linear probing |
| LIST | O(n) | O(n) | Scansione completa |
| SAVE | O(n) | O(n) | Scrittura sequenziale |
| LOAD | O(n) | O(n) | Lettura sequenziale |

n = numero di record

## Memory Layout

```
Stack (main):
┌──────────────────┐
│ db pointer       │ ───────┐
└──────────────────┘        │
                           │
Heap (dynamically allocated):
                           ▼
┌─────────────────────────────────────────────┐
│ HashTable                                   │
│  ┌───┬───┬───┬───┬───┬───┬───┬───┬───┐      │
│  │ 0 │ 1 │ 2 │ 3 │ 4 │ 5 │...│1023│    │      │
│  └───┴───┴───┴───┴───┴───┴───┴───┴───┘      │
│    │                   │                   │
│    ▼                   ▼                   │
│  ┌─────┐           ┌─────┐                 │
│  │ R1  │           │ R2  │                 │
│  │ key │           │ key │                 │
│  │ val │           │ val │                 │
│  │ ts  │           │ ts  │                 │
│  └─────┘           └─────┘                 │
└─────────────────────────────────────────────┘
```

## Thread Safety

⚠️ **ATTENZIONE**: Questa implementazione NON è thread-safe.

Non utilizzare in ambienti multi-threaded senza:
- Mutex per proteggere l'accesso alla hash table
- Lock per le operazioni di I/O file
- Condizioni per la sincronizzazione

## Error Handling

Strategia adottata:
1. **Parametri NULL**: Check all'inizio delle funzioni
2. **Return Values**: -1 per errore, 0 per successo
3. **Messages**: fprintf(stderr, ...) per errori
4. **Memory**: Check malloc/fmalloc return values

## Possibili Ottimizzazioni

### 1. Rehashing Dinamico
```c
// Quando load factor > 0.7
if ((float)table->count / TABLE_SIZE > 0.7) {
    db_resize(table, TABLE_SIZE * 2);
}
```

### 2. Lazy Deletion
```c
typedef enum { ACTIVE, DELETED } RecordStatus;

typedef struct {
    char key[MAX_KEY_LENGTH];
    char value[MAX_VALUE_LENGTH];
    time_t timestamp;
    RecordStatus status;
} Record;
```

### 3. Compressione
```c
// Usare zlib per comprimere il file
#include <zlib.h>

int db_save_compressed(HashTable* table, const char* filename) {
    // Implementazione compressione
}
```

### 4. Cache delle Chiavi
```c
// Cache LRU per GET frequenti
typedef struct {
    char* key;
    char* value;
} CacheEntry;
```

## Dipendenze

Nessuna dipendenza esterna! Solo librerie standard C:
- `<stdio.h>` - I/O
- `<stdlib.h>` - malloc/free
- `<string.h>` - strlen/strcmp
- `<time.h>` - timestamp
- `<ctype.h>` - character handling

## Portabilità

Codice 100% ANSI C99:
- ✅ Linux (GCC, Clang)
- ✅ macOS (Clang)
- ✅ Windows (MinGW, MSVC)
- ✅ BSD (Clang, GCC)
- ✅ Embedded (con libc)
